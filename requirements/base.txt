# Runtime dependencies. Pinned exactly — a 10-year system should not have its
# behaviour change because a transitive release shifted underneath it.
# Upgrade deliberately, with the test suite as the gate.

# Django 5.2 is the current LTS (security support to April 2028). Chosen over
# the newer 6.x line, which is not LTS and reaches EOL much sooner.
Django==5.2.17

djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1

# PostgreSQL adapter (psycopg 3).
psycopg[binary]==3.3.4

django-cors-headers==4.9.0
django-filter==26.1
django-environ==0.14.0

# OpenAPI schema + interactive docs at /api/schema/, /api/docs/.
drf-spectacular==0.28.0
